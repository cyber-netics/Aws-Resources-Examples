{
  "scripts": {
    "build": "tsc",
    "test": "jest ./test",
    "cdk:deploy": "cd cdk && npm run deploy",
    "setup": "npm run setup:cdk && npm run setup:app",
    "setup:cdk": "cd cdk && npm install",
    "setup:app": "npm install"
  },
  "devDependencies": {
    "@types/node": "^15.12.1",
    "dotenv": "^10.0.0",
    "ts-node": "^10.0.0"
  }
}
